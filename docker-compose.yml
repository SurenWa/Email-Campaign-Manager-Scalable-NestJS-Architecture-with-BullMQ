# Docker Compose Configuration for Email Campaign Manager when nest js app runs locally
# version: '3.8'

# services:
#   # ============================================
#   # PostgreSQL Database
#   # ============================================
#   postgres:
#     image: postgres:16-alpine
#     container_name: email-campaign-postgres
#     restart: unless-stopped
#     environment:
#       POSTGRES_USER: postgres
#       POSTGRES_PASSWORD: postgres
#       POSTGRES_DB: email_campaign
#     ports:
#       - '5432:5432'
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ['CMD-SHELL', 'pg_isready -U postgres']
#       interval: 10s
#       timeout: 5s
#       retries: 5
#     networks:
#       - email-campaign-network

#   # ============================================
#   # Redis Queue Storage
#   # ============================================
#   redis:
#     image: redis:7-alpine
#     container_name: email-campaign-redis
#     restart: unless-stopped
#     ports:
#       - '6379:6379'
#     volumes:
#       - redis_data:/data
#     healthcheck:
#       test: ['CMD', 'redis-cli', 'ping']
#       interval: 10s
#       timeout: 5s
#       retries: 5
#     command: redis-server --appendonly yes
#     networks:
#       - email-campaign-network

#   # ============================================
#   # API Server (Development)
#   # ============================================
#   api:
#     build:
#       context: .
#       dockerfile: Dockerfile.dev
#     container_name: email-campaign-api
#     restart: unless-stopped
#     ports:
#       - '3000:3000'
#     environment:
#       NODE_ENV: development
#       PORT: 3000
#       API_PREFIX: api/v1
#       DATABASE_URL: postgresql://postgres:postgres@postgres:5432/email_campaign?schema=public
#       REDIS_HOST: redis
#       REDIS_PORT: 6379
#       JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
#       JWT_EXPIRATION: ${JWT_EXPIRATION:-86400}
#       SENDGRID_API_KEY: ${SENDGRID_API_KEY}
#       SENDGRID_FROM_EMAIL: ${SENDGRID_FROM_EMAIL}
#       SENDGRID_FROM_NAME: ${SENDGRID_FROM_NAME:-Email Campaign Manager}
#     volumes:
#       - .:/app
#       - /app/node_modules
#     depends_on:
#       postgres:
#         condition: service_healthy
#       redis:
#         condition: service_healthy
#     command: npm run start:dev
#     networks:
#       - email-campaign-network

#   # ============================================
#   # Worker Process (Development)
#   # ============================================
#   worker:
#     build:
#       context: .
#       dockerfile: Dockerfile.dev
#     container_name: email-campaign-worker
#     restart: unless-stopped
#     environment:
#       NODE_ENV: development
#       DATABASE_URL: postgresql://postgres:postgres@postgres:5432/email_campaign?schema=public
#       REDIS_HOST: redis
#       REDIS_PORT: 6379
#       JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
#       JWT_EXPIRATION: ${JWT_EXPIRATION:-86400}
#       SENDGRID_API_KEY: ${SENDGRID_API_KEY}
#       SENDGRID_FROM_EMAIL: ${SENDGRID_FROM_EMAIL}
#       SENDGRID_FROM_NAME: ${SENDGRID_FROM_NAME:-Email Campaign Manager}
#     volumes:
#       - .:/app
#       - /app/node_modules
#     depends_on:
#       postgres:
#         condition: service_healthy
#       redis:
#         condition: service_healthy
#     command: npm run worker:dev
#     networks:
#       - email-campaign-network

# volumes:
#   postgres_data:
#     name: email-campaign-postgres-data
#   redis_data:
#     name: email-campaign-redis-data

# networks:
#   email-campaign-network:
#     name: email-campaign-network
#     driver: bridge


## Docker Compose Configuration for Email Campaign Manager (dev when nest js dockerized)

# version: '3.8'

# services:
#   # ============================================
#   # PostgreSQL Database
#   # ============================================
#   postgres:
#     image: postgres:16-alpine
#     container_name: email-campaign-postgres
#     restart: unless-stopped
#     environment:
#       POSTGRES_USER: ${DB_USER:-postgres}
#       POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
#       POSTGRES_DB: ${DB_NAME:-email_campaign}
#     ports:
#       - '5432:5432'
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-postgres}']
#       interval: 10s
#       timeout: 5s
#       retries: 5
#     networks:
#       - email-campaign-network

#   # ============================================
#   # Redis Queue Storage
#   # ============================================
#   redis:
#     image: redis:7-alpine
#     container_name: email-campaign-redis
#     restart: unless-stopped
#     ports:
#       - '6379:6379'
#     volumes:
#       - redis_data:/data
#     healthcheck:
#       test: ['CMD', 'redis-cli', '-a', '${REDIS_PASSWORD:-dev-redis-password}', 'ping']
#       interval: 10s
#       timeout: 5s
#       retries: 5
#     command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-dev-redis-password}
#     networks:
#       - email-campaign-network

#   # ============================================
#   # API Server
#   # ============================================
#   api:
#     build:
#       context: .
#       dockerfile: Dockerfile.dev
#     container_name: email-campaign-api
#     restart: unless-stopped
#     ports:
#       - '${API_PORT:-3000}:3000'
#     environment:
#       NODE_ENV: ${NODE_ENV:-development}
#       PORT: 3000
#       API_PREFIX: api/v1
#       DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@postgres:5432/${DB_NAME:-email_campaign}?schema=public
#       REDIS_HOST: redis
#       REDIS_PORT: 6379
#       REDIS_PASSWORD: ${REDIS_PASSWORD:-dev-redis-password}
#       JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
#       JWT_EXPIRATION: ${JWT_EXPIRATION:-86400}
#       SENDGRID_API_KEY: ${SENDGRID_API_KEY}
#       SENDGRID_FROM_EMAIL: ${SENDGRID_FROM_EMAIL}
#       SENDGRID_FROM_NAME: ${SENDGRID_FROM_NAME:-Email Campaign Manager}
#     volumes:
#       - .:/app
#       - /app/node_modules
#     depends_on:
#       postgres:
#         condition: service_healthy
#       redis:
#         condition: service_healthy
#     command: npm run start:dev
#     networks:
#       - email-campaign-network

#   # ============================================
#   # Worker Process
#   # ============================================
#   worker:
#     build:
#       context: .
#       dockerfile: Dockerfile.dev
#     container_name: email-campaign-worker
#     restart: unless-stopped
#     environment:
#       NODE_ENV: ${NODE_ENV:-development}
#       DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@postgres:5432/${DB_NAME:-email_campaign}?schema=public
#       REDIS_HOST: redis
#       REDIS_PORT: 6379
#       REDIS_PASSWORD: ${REDIS_PASSWORD:-dev-redis-password}
#       JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
#       JWT_EXPIRATION: ${JWT_EXPIRATION:-86400}
#       SENDGRID_API_KEY: ${SENDGRID_API_KEY}
#       SENDGRID_FROM_EMAIL: ${SENDGRID_FROM_EMAIL}
#       SENDGRID_FROM_NAME: ${SENDGRID_FROM_NAME:-Email Campaign Manager}
#     volumes:
#       - .:/app
#       - /app/node_modules
#     depends_on:
#       postgres:
#         condition: service_healthy
#       redis:
#         condition: service_healthy
#     command: npm run worker:dev
#     networks:
#       - email-campaign-network

# volumes:
#   postgres_data:
#     name: email-campaign-postgres-data
#   redis_data:
#     name: email-campaign-redis-data

# networks:
#   email-campaign-network:
#     name: email-campaign-network
#     driver: bridge


## Docker Compose Configuration for Email Campaign Manager (dev when nest js dockerized and with prometheus & grafana)
version: '3.8'

services:
  # ============================================
  # PostgreSQL Database
  # ============================================
  postgres:
    image: postgres:16-alpine
    container_name: email-campaign-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-email_campaign}
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${DB_USER:-postgres}']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - email-campaign-network

  # ============================================
  # Redis Queue Storage
  # ============================================
  redis:
    image: redis:7-alpine
    container_name: email-campaign-redis
    restart: unless-stopped
    ports:
      - '6379:6379'
    volumes:
      - redis_data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', '-a', '${REDIS_PASSWORD:-dev-redis-password}', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD:-dev-redis-password}
    networks:
      - email-campaign-network

  # ============================================
  # API Server
  # ============================================
  api:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: email-campaign-api
    restart: unless-stopped
    ports:
      - '${API_PORT:-3000}:3000'
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      PORT: 3000
      API_PREFIX: api/v1
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@postgres:5432/${DB_NAME:-email_campaign}?schema=public
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-dev-redis-password}
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
      JWT_EXPIRATION: ${JWT_EXPIRATION:-86400}
      SENDGRID_API_KEY: ${SENDGRID_API_KEY}
      SENDGRID_FROM_EMAIL: ${SENDGRID_FROM_EMAIL}
      SENDGRID_FROM_NAME: ${SENDGRID_FROM_NAME:-Email Campaign Manager}
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: npm run start:dev
    networks:
      - email-campaign-network

  # ============================================
  # Worker Process
  # ============================================
  worker:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: email-campaign-worker
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      DATABASE_URL: postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@postgres:5432/${DB_NAME:-email_campaign}?schema=public
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-dev-redis-password}
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
      JWT_EXPIRATION: ${JWT_EXPIRATION:-86400}
      SENDGRID_API_KEY: ${SENDGRID_API_KEY}
      SENDGRID_FROM_EMAIL: ${SENDGRID_FROM_EMAIL}
      SENDGRID_FROM_NAME: ${SENDGRID_FROM_NAME:-Email Campaign Manager}
    volumes:
      - .:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    command: npm run worker:dev
    networks:
      - email-campaign-network

  # ============================================
  # Prometheus (Metrics Collection)
  # ============================================
  prometheus:
    image: prom/prometheus:v2.47.0
    container_name: email-campaign-prometheus
    restart: unless-stopped
    ports:
      - '9090:9090'
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.enable-lifecycle'
    depends_on:
      - api
    networks:
      - email-campaign-network

  # ============================================
  # Grafana (Metrics Visualization)
  # ============================================
  grafana:
    image: grafana/grafana:10.1.0
    container_name: email-campaign-grafana
    restart: unless-stopped
    ports:
      - '3001:3000'
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: 'false'
    volumes:
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - email-campaign-network

volumes:
  postgres_data:
    name: email-campaign-postgres-data
  redis_data:
    name: email-campaign-redis-data
  prometheus_data:
    name: email-campaign-prometheus-data
  grafana_data:
    name: email-campaign-grafana-data

networks:
  email-campaign-network:
    name: email-campaign-network
    driver: bridge